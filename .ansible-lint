---
use_default_rules: true
profile: production
skip_list:
  - yaml[line-length]  # Allow long lines
  - key-order  # Cosmetic issue, not a bug
  - risky-file-permissions  # File permissions are managed elsewhere

exclude_paths:
  - .github/
  - .cache/
  - molecule/  # Molecule tests are run separately
