---
- name: Converge the stable platform contract
  hosts: all
  become: true
  tasks:
    - name: Apply the role from this checkout
      ansible.builtin.include_role:
        name: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') }}"
