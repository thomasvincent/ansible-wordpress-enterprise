<?php
// Generated by Ansible - Do not edit manually
// Written only when wordpress_debug is set. Reports state, never secrets.

require_once __DIR__ . '/wp-load.php';

// Refuse to serve if this file outlives the debug flag it was written under.
if (!defined('WP_DEBUG') || !WP_DEBUG) {
    status_header(404);
    exit;
}

header('Content-Type: text/plain; charset=utf-8');

printf("Site URL:       %s\n", get_site_url());
printf("Home URL:       %s\n", get_home_url());
printf("WordPress:      %s\n", get_bloginfo('version'));
printf("PHP:            %s\n", PHP_VERSION);
printf("Theme:          %s\n", wp_get_theme()->get('Name'));
printf("Active plugins: %d\n", count((array) get_option('active_plugins', array())));
printf("Multisite:      %s\n", is_multisite() ? 'yes' : 'no');
printf("Debug:          %s\n", (defined('WP_DEBUG') && WP_DEBUG) ? 'on' : 'off');
