# Python dependencies for ansible-wordpress-enterprise

# Core dependencies
ansible>=14.3.1,<15.0.0
ansible-core>=2.21.3,<2.22.0
ansible-lint>=26.8.0
yamllint>=1.38.0

# Testing dependencies
molecule>=26.8.0
molecule-plugins[docker]>=26.7.15
pytest>=9.1.1
pytest-testinfra>=10.2.2
PyYAML>=6.0.3

# Ansible collections (installed via ansible-galaxy, listed for reference)
# ansible-galaxy collection install community.general
# ansible-galaxy collection install community.mysql
# ansible-galaxy collection install ansible.posix
