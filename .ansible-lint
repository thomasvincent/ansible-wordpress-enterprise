---
use_default_rules: true
profile: production

# Long lines are unavoidable in URL and Jinja values, and folding them into
# block scalars changes the value. Every other rule is enforced.
skip_list:
  - yaml[line-length]

exclude_paths:
  - .github/
  - .cache/
  - molecule/  # Molecule tests are run separately
